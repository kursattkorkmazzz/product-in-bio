"use server";
import { SocialMediaType } from "@/app/dashboard/link-editor/types/social-media";

export default async function getSocialMediaLinks(
  userId: string
): Promise<SocialMediaType[]> {
  return [];
}
